# CLI Guide - Telegram Outreach без Google Sheets и n8n

Полное руководство по работе через командную строку.

## 🚀 Быстрый старт (3 шага)

### 1. Добавить аккаунт

```bash
python cli_menu.py
# Выбрать: 1
```

**Или напрямую:**
```bash
python add_account_cli.py
```

### 2. Добавить пользователей

```bash
python cli_menu.py
# Выбрать: 2
```

**Или напрямую:**
```bash
python add_users_cli.py examples/users_example.json
```

### 3. Запустить рассылку

```bash
python cli_menu.py
# Выбрать: 4
```

**Или напрямую:**
```bash
python start_outreach_cli.py
```

---

## 📚 Детальное руководство

### Объединенное меню (рекомендуется)

```bash
python cli_menu.py
```

**Возможности:**
- ✅ Dashboard с статистикой
- ✅ Добавление аккаунтов
- ✅ Управление списком пользователей
- ✅ Запуск рассылки
- ✅ Тестирование
- ✅ Просмотр данных

---

## 1️⃣ Работа с аккаунтами

### Способы добавления:

#### A. Из существующей сессии (create_session.py)

```bash
python add_account_cli.py
# Выбрать: 4 (Current session)
```

#### B. Из TDATA (Telegram Desktop)

```bash
python add_account_cli.py
# Выбрать: 1
# Ввести путь: /path/to/tdata.zip
```

#### C. Из .session файла

```bash
python add_account_cli.py /path/to/account.session
```

#### D. Из JSON credentials

**Создать файл** `account.json`:
```json
{
  "phone": "+79991234567",
  "api_id": 12345678,
  "api_hash": "your_api_hash",
  "password": "2fa_password_optional"
}
```

**Добавить:**
```bash
python add_account_cli.py account.json
```

---

## 2️⃣ Управление списком пользователей

### Добавить одного пользователя (интерактивно)

```bash
python add_users_cli.py
# Выбрать: 1
# Ввести: username или user_id
```

### Добавить из JSON файла

**Создать** `users.json`:
```json
[
  {
    "user_id": 7115610560,
    "username": "champfreak",
    "source": "Target",
    "priority": 10
  },
  {
    "username": "durov",
    "source": "Manual",
    "priority": 8
  }
]
```

**Добавить:**
```bash
python add_users_cli.py users.json
```

### Добавить из TXT файла

**Создать** `users.txt`:
```
# Format: username/user_id [source] [priority]
champfreak Target 10
durov Manual 8
telegram Manual 7
123456789 LinkedIn 5
```

**Добавить:**
```bash
python add_users_cli.py users.txt
```

### Посмотреть список

```bash
python add_users_cli.py --list
```

### Очистить список

```bash
python add_users_cli.py --clear
```

---

## 3️⃣ Запуск рассылки

### Интерактивный режим

```bash
python start_outreach_cli.py
# Выбрать: 1
# Ввести сообщение
# Подтвердить
```

### С дефолтным сообщением (первые 3 юзера)

```bash
python start_outreach_cli.py
# Выбрать: 2
```

### Dry run (тест без отправки)

```bash
python start_outreach_cli.py --dry-run 5
```

Или:
```bash
python start_outreach_cli.py
# Выбрать: 4
```

### Посмотреть статистику

```bash
python start_outreach_cli.py --stats
```

---

## 📊 Структура данных

Все данные хранятся в **test_data.json**:

```json
{
  "accounts": [
    {
      "id": "acc_1",
      "phone": "+79991234567",
      "status": "active",
      "daily_sent": 5,
      "total_sent": 100
    }
  ],
  "users": [
    {
      "user_id": 7115610560,
      "username": "champfreak",
      "status": "pending",
      "priority": 10
    }
  ],
  "dialogs": [
    {
      "user_id": 7115610560,
      "account_id": "acc_1",
      "stage": 1,
      "status": "active"
    }
  ],
  "logs": []
}
```

### Посмотреть данные:

```bash
cat test_data.json | jq '.'
```

---

## 🎯 Примеры использования

### Сценарий 1: Отправить 10 пользователям

```bash
# 1. Добавить аккаунт (если нет)
python create_session.py
python add_account_cli.py
# Выбрать: 4

# 2. Добавить пользователей
python add_users_cli.py examples/users_example.json

# 3. Запустить рассылку
python start_outreach_cli.py
# Выбрать: 1
# Ввести сообщение
```

### Сценарий 2: Тестирование (dry run)

```bash
# 1. Добавить тестовых пользователей
python add_users_cli.py examples/users_example.txt

# 2. Dry run (без реальной отправки)
python start_outreach_cli.py --dry-run 3

# 3. Проверить что все ОК
python start_outreach_cli.py --stats
```

### Сценарий 3: Использование через меню

```bash
# Единая точка входа
python cli_menu.py

# Dashboard покажет текущее состояние
# Выбрать нужную операцию из меню
```

---

## 🔧 Настройки рассылки

### Задержка между сообщениями

По умолчанию: **30-90 секунд** (рандомно)

Изменить в `start_outreach_cli.py`:
```python
await run_outreach(delay_min=60, delay_max=120)
```

### Лимиты отправки

Настраиваются в `.env` или `test_data.json` (Settings):

```bash
MAX_DAILY_ACTIVE=7     # Для active аккаунтов
MAX_DAILY_WARMING=3    # Для warming аккаунтов
```

### Персонализация сообщений

Используйте placeholder `{name}`:

```
Привет, {name}!

Наткнулся на твой профиль...
```

Система заменит `{name}` на username пользователя.

---

## 📁 Файловая структура

```
telespam/
├── cli_menu.py                 # 🎯 Главное меню (начать отсюда)
├── add_account_cli.py          # Добавление аккаунтов
├── add_users_cli.py            # Управление списком
├── start_outreach_cli.py       # Запуск рассылки
├── test_data.json             # 💾 Все данные здесь
├── examples/
│   ├── users_example.json     # Пример списка (JSON)
│   ├── users_example.txt      # Пример списка (TXT)
│   └── account_credentials.json # Пример credentials
└── sessions/                   # Session файлы
    └── +79991234567.session
```

---

## 🎨 Персонализация

### Разные сообщения для разных пользователей

Вручную через API или модифицировав `start_outreach_cli.py`:

```python
# В функции run_outreach():
if user.get('source') == 'LinkedIn':
    message_text = "Привет! Нашел тебя на LinkedIn..."
elif user.get('source') == 'Twitter':
    message_text = "Привет! Читаю твой Twitter..."
else:
    message_text = default_message
```

### Мультиэтапные воронки

Пока не реализовано в CLI, но можно:
1. Запустить первую волну
2. Подождать ответов
3. Вручную перевести диалоги на stage 2
4. Запустить вторую волну

---

## 📊 Мониторинг

### Статистика в реальном времени

```bash
# Во время рассылки
watch -n 5 "python start_outreach_cli.py --stats"
```

### Логи

```bash
# Последние 10 событий
python -c "
from mock_sheets import sheets_manager
for log in sheets_manager.logs[-10:]:
    print(f\"{log.get('action')}: {log.get('result')}\")
"
```

### Просмотр прогресса

```bash
python cli_menu.py
# Выбрать: 3 (View system status)
```

---

## ⚠️ Лимиты и рекомендации

### Telegram лимиты:
- **Новые аккаунты:** 30-40 сообщений/день
- **Прогретые:** 50-100 сообщений/день
- **Старые аккаунты:** 200+ сообщений/день

### Рекомендации:
- ✅ Задержка **60-90 сек** между сообщениями
- ✅ Начинать с **5-10 сообщений/день**
- ✅ Использовать **разные тексты**
- ✅ Прогревать новые аккаунты **5-7 дней**
- ⚠️ Не спамить - отписываться от негатива
- ⚠️ Использовать прокси при >50 сообщений/день

---

## 🐛 Troubleshooting

### "No available accounts"

```bash
# Проверить аккаунты
python test_terminal.py
# Выбрать: 6

# Добавить аккаунт
python add_account_cli.py
```

### "No pending users"

```bash
# Добавить пользователей
python add_users_cli.py examples/users_example.json
```

### Flood error

Система автоматически:
- Переключается на следующий аккаунт
- Устанавливает cooldown

Решение: добавить больше аккаунтов или увеличить задержки.

### Session expired

```bash
# Пересоздать сессию
python create_session.py

# Обновить в системе
python add_account_cli.py
```

---

## 💡 Советы

1. **Начните с dry run:**
   ```bash
   python start_outreach_cli.py --dry-run 3
   ```

2. **Используйте приоритеты:**
   - 10 = самые важные контакты
   - 5 = обычные
   - 1 = низкий приоритет

3. **Проверяйте статус регулярно:**
   ```bash
   python cli_menu.py
   ```

4. **Бэкапьте данные:**
   ```bash
   cp test_data.json backup_$(date +%Y%m%d).json
   ```

5. **Тестируйте на себе сначала:**
   - Добавьте свой второй аккаунт в список
   - Проверьте что сообщения приходят корректно

---

## 🎯 Что дальше?

После освоения CLI:
1. Настроить Google Sheets (опционально)
2. Настроить n8n для автоматизации (опционально)
3. Добавить прокси для масштабирования
4. Создать мультиэтапные воронки

---

## 📞 Быстрая справка

```bash
# Главное меню
python cli_menu.py

# Добавить аккаунт
python add_account_cli.py

# Добавить пользователей
python add_users_cli.py examples/users_example.json

# Запустить рассылку
python start_outreach_cli.py

# Dry run
python start_outreach_cli.py --dry-run 5

# Статистика
python start_outreach_cli.py --stats

# Просмотр данных
cat test_data.json | jq '.'
```

---

**Готово!** Начните с `python cli_menu.py` 🚀
