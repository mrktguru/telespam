# 🔧 ИСПРАВЛЕНИЕ: CSV/XLS импорт не работает

## 🎯 Проблема

При нажатии на кнопку "Import CSV/XLS" появляется ошибка:
```
Uncaught ReferenceError: handleCampaignCSVUpload is not defined
```

## ✅ Решение

### ШАГ 1: Очистите кэш браузера (ОБЯЗАТЕЛЬНО!)

**Самый быстрый способ - Жесткая перезагрузка:**

- **Windows/Linux:** `Ctrl + Shift + R`
- **Mac:** `Cmd + Shift + R`

**Или через DevTools:**

1. Откройте страницу с кампанией
2. Нажмите `F12` (откроется DevTools)
3. **Правой кнопкой мыши** на кнопку обновления браузера
4. Выберите **"Empty Cache and Hard Reload"**

![Hard Reload](https://i.imgur.com/xxx.png)

### ШАГ 2: Проверьте, что ошибка исчезла

1. Оставьте DevTools открытым (`F12`)
2. Перейдите на вкладку **Console**
3. Нажмите "Import CSV/XLS"
4. Выберите файл

**✅ ДОЛЖНО быть:**
```
Processing test_users.csv...
```

**❌ НЕ ДОЛЖНО быть:**
```
handleCampaignCSVUpload is not defined
```

### ШАГ 3: Если не помогло - Режим инкогнито

1. Откройте **новое окно в режиме инкогнито:**
   - Chrome/Edge: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`

2. Войдите на сайт заново
3. Попробуйте импортировать CSV

---

## 📋 Полный чеклист исправления

- [ ] Выполнил `Ctrl + Shift + R` (жесткая перезагрузка)
- [ ] Открыл Console (F12) - нет ошибки "handleCampaignCSVUpload"
- [ ] Нажал "Import CSV/XLS" - появился file picker
- [ ] Выбрал CSV файл
- [ ] В Console появилось "Processing..."
- [ ] Увидел сообщение "Successfully imported X users!"

---

## 🔍 Как убедиться что кэш очищен

В консоли браузера (F12 → Console) выполните:

```javascript
typeof handleCSVUpload
```

**Результат должен быть:** `"function"`

Если `"undefined"` - кэш еще не очищен, повторите Шаг 1.

---

## 🆘 Если НИЧЕГО не помогает

### На Production сервере:

```bash
# 1. Убедитесь что код обновлен
cd /path/to/telespam
git pull origin claude/audit-project-changes-01RhbB1KccoAFLjDsmGAnVR9

# 2. Проверьте версию
git log -1 --oneline
# Должно быть: 05652ef или новее

# 3. Перезапустите сервис
sudo systemctl restart telespam-web

# 4. Проверьте статус
sudo systemctl status telespam-web
```

### Проверка файла:

```bash
grep -n "function handleCSVUpload" templates/new_campaign.html
```

**Должно показать:**
```
231:function handleCSVUpload(event) {
```

---

## 🎓 Почему это происходит?

Браузеры кэшируют JavaScript код для ускорения загрузки. Когда мы обновили код на сервере, браузер продолжал использовать старую версию из кэша.

В старой версии функция называлась `handleCampaignCSVUpload`, а в новой - `handleCSVUpload`.

**Жесткая перезагрузка** заставляет браузер скачать свежую версию.

---

## ✅ После исправления

Импорт CSV/XLS должен работать так:

1. Нажимаете "Import CSV/XLS" → открывается file picker
2. Выбираете файл → появляется "Processing test_users.csv..."
3. Через 1-2 секунды → "Successfully imported 3 users! Reloading..."
4. Страница перезагружается → пользователи в списке

---

**Версия инструкции:** 1.0
**Дата:** 2025-11-30
**Коммит:** 05652ef
