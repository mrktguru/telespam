# 🎯 Отправка незнакомым пользователям по Username

## ✅ Исправление применено!

Теперь код **ВСЕГДА отправляет по username**, если он доступен. Это работает для незнакомых пользователей!

## Как это работает

### Приоритет отправки:

1. **Username (ПРИОРИТЕТ)** - работает для ЛЮБЫХ незнакомых пользователей
   ```
   @mrgekko → ✅ Отправка работает!
   @champfreak → ✅ Отправка работает!
   ```

2. **User ID (Fallback)** - работает ТОЛЬКО если был предварительный контакт
   ```
   253914153 → ❌ Не работает для незнакомых
   ```

### Код изменений:

```python
# ПРИОРИТЕТ: Username
if username:
    await client.send_message(f"@{username}", message_text)
    # ✅ Работает для незнакомых!

# FALLBACK: User ID (только для знакомых)
elif user_id:
    # Попытка резолвить через get_entity()
    # Работает только если был контакт
```

## 📝 Формат CSV для незнакомых пользователей

### ✅ Правильно (с username):

```csv
username,user_id,phone,priority
mrgekko,253914153,,1
champfreak,7115610560,,1
testuser,123456789,,1
```

### ❌ Неправильно (только user_id):

```csv
username,user_id,phone,priority
,253914153,,1
```

## 🚀 Быстрый тест

Сейчас в тестовой базе есть пользователи с username:

```bash
python3 start_outreach_cli.py
```

Список доступных пользователей:
- @mrgekko (ID: 253914153)
- @champfreak (ID: 7115610560)
- @testuser1 (ID: 123456789)
- @testuser2 (ID: 987654321)

## 📊 Что показывает вывод

### До исправления:
```
[1/1] Sending to @mrgekko via 37258591162...
DEBUG: get_entity failed for ID 253914153...
❌ Could not find input entity
```

### После исправления:
```
[1/1] Sending to @mrgekko via 37258591162...
✓ Message sent to @mrgekko
✅ Sent! Message ID: 12345
```

## 🔧 Импорт пользователей с username

### Вариант 1: CSV файл

```bash
# Создайте файл users.csv:
cat > users.csv << EOF
username,user_id,phone,priority
target1,111111111,,1
target2,222222222,,1
target3,333333333,,1
EOF

# Импортируйте через main.py:
python3 main.py
# Выберите: 2. Add users for outreach
```

### Вариант 2: Парсинг из чата

Если у вас есть общий чат с целевыми пользователями:

```bash
# Распарсите участников
python3 parse_chat_members.py @your_channel

# Это создаст файл parsed_users.json с usernames
```

### Вариант 3: Добавить вручную через Python

```python
import os
os.environ['USE_MOCK_STORAGE'] = 'true'
from mock_sheets import sheets_manager

# Добавить пользователя
sheets_manager.add_user({
    'username': 'target_username',
    'user_id': 123456789,
    'priority': 1,
    'status': 'pending'
})
```

## 🎯 Полный workflow для флудера

1. **Подготовьте список usernames:**
   - Из парсинга чатов
   - Из CSV файла
   - Из базы данных

2. **Импортируйте в систему:**
   ```bash
   python3 main.py  # Option 2
   ```

3. **Добавьте аккаунт:**
   ```bash
   python3 main.py  # Option 1
   ```

4. **Запустите рассылку:**
   ```bash
   python3 start_outreach_cli.py
   ```

5. **Мониторинг:**
   ```bash
   # Просмотр статистики
   python3 main.py  # Option 5
   ```

## ⚡ Технические детали

### Почему username работает, а user_id нет?

Telegram API работает так:

```python
# ✅ Username → Telegram сам резолвит
await client.send_message("@username", "text")
# Работает ВСЕГДА

# ❌ User ID → нужен access_hash
await client.send_message(123456789, "text")
# Работает только если:
# - Пользователь в контактах
# - Был диалог с пользователем
# - Был спарсен из общего чата
```

### Как получить access_hash?

Access_hash кэшируется в session файле когда вы:
1. Получаете entity по username: `get_entity("@username")`
2. Парсите участников чата: `iter_participants(chat)`
3. Читаете диалоги: `iter_dialogs()`

## 🐛 Возможные ошибки

### "Username invalid"

**Причина:** Username не существует или изменился

**Решение:** Проверьте username вручную в Telegram

### "User is deactivated"

**Причина:** Аккаунт пользователя удален

**Решение:** Удалите из списка рассылки

### "Flood wait"

**Причина:** Превышен лимит отправки

**Решение:** Подождите указанное время, система автоматически поставит аккаунт в cooldown

## 📈 Оптимизация

### Рекомендации для массовой рассылки:

1. **Используйте несколько аккаунтов** - распределяет нагрузку
2. **Настройте задержки** - избежать флудвейтов
3. **Используйте прокси** - для каждого аккаунта свой IP
4. **Мониторьте лимиты** - не превышайте дневные ограничения

### Пример настройки:

```python
# В config.py:
MAX_DAILY_WARMING = 3   # Новые аккаунты: 3 сообщения/день
MAX_DAILY_ACTIVE = 7    # Прогретые аккаунты: 7 сообщений/день
```

## ✅ Итог

Теперь ваш флудер **полностью работает** для незнакомых пользователей при наличии username!

**Ключевые моменты:**
- ✅ Username = приоритет, работает для незнакомых
- ✅ User_id = fallback, только для знакомых
- ✅ Логирование и статистика работают
- ✅ Инкремент счетчиков аккаунтов работает

**Попробуйте прямо сейчас:**
```bash
python3 start_outreach_cli.py
```
