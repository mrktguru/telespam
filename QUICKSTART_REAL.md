# Quick Start - Real Message Sending

Отправка реальных сообщений через Telegram за 3 шага.

## 📋 Предварительные требования

1. **Telegram аккаунт** (ваш основной)
2. **API credentials** от Telegram

### Получить API credentials:

1. Откройте https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в **"API development tools"**
4. Создайте приложение (если нет):
   - App title: `My Outreach System`
   - Short name: `outreach`
   - Platform: `Desktop`
5. Скопируйте:
   - **api_id** (число)
   - **api_hash** (строка)

## 🚀 Шаг 1: Создать сессию

```bash
python create_session.py
```

Вас попросят ввести:
1. ✅ API ID
2. ✅ API Hash
3. ✅ Ваш номер телефона (с кодом страны: +79991234567)
4. ✅ Код из Telegram (придет в приложение)
5. ✅ 2FA пароль (если включен)

**Результат:**
```
✅ SUCCESS! Session created
Phone: +79991234567
Username: @yourname
Session file: /path/to/sessions/+79991234567.session
```

## 📱 Шаг 2: Отправить тестовое сообщение

```bash
python test_send_message.py
```

**Вопросы:**
1. Использовать дефолтного получателя @champfreak (ID: 7115610560)? → **yes**
2. Тип сообщения (1-текст, 2-фото, 3-видео) → **1**
3. Использовать дефолтный текст? → **yes**

**Результат:**
```
✅ MESSAGE SENT SUCCESSFULLY!
Message ID: 12345
Date: 2024-01-15 14:30:00
Text: 🤖 Test message from Telegram Outreach System!
```

## 🎯 Шаг 3: Добавить в систему (опционально)

Если хотите использовать через API:

```bash
export USE_MOCK_STORAGE=true
python add_account_to_system.py
```

Теперь можно использовать через API:

```bash
# Запустить сервер
python main.py

# В другом терминале
curl -X POST http://localhost:8000/send \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 7115610560,
    "type": "text",
    "content": "Hello from API!"
  }'
```

## 📝 Примеры использования

### Отправить свой текст

```bash
python test_send_message.py
# Выбрать: yes (default target)
# Выбрать: 1 (text)
# Выбрать: yes (custom message)
# Ввести ваш текст
```

### Отправить другому пользователю

```bash
python test_send_message.py
# Выбрать: no (custom target)
# Ввести: username или user_id
# Далее как обычно
```

### Отправить фото

```bash
python test_send_message.py
# Выбрать: yes (default target)
# Выбрать: 2 (photo)
# Ввести путь к фото: /path/to/photo.jpg
# Ввести подпись (опционально)
```

## 🔍 Проверка статуса

### Просмотреть сессии:

```bash
ls -la sessions/
```

### Проверить .env:

```bash
cat .env
```

Должно быть:
```bash
API_ID=12345678
API_HASH=abcdef123456...
USE_MOCK_STORAGE=false  # или true для тестов
```

### Проверить аккаунт в системе:

```bash
export USE_MOCK_STORAGE=true
python -c "
from mock_sheets import sheets_manager
sheets_manager.print_summary()
"
```

## ⚠️ Troubleshooting

### "API credentials not found"
```bash
# Проверить .env
cat .env

# Или установить вручную
export API_ID=12345678
export API_HASH=your_hash
```

### "Session expired"
```bash
# Пересоздать сессию
python create_session.py
```

### "Could not find user"
```bash
# Убедитесь что username правильный (без @)
# Или используйте User ID напрямую
```

### "Telethon not installed"
```bash
pip install telethon
```

## 📊 Целевой пользователь (ваш)

По умолчанию настроено на:
- **Username:** @champfreak
- **User ID:** 7115610560
- **Phone:** +358 45 8932804

Можно отправить на любой другой аккаунт, указав username или ID.

## 🎨 Типы сообщений

### 1. Текстовое сообщение
```python
python test_send_message.py
# Type: 1
```

### 2. Фото с подписью
```python
python test_send_message.py
# Type: 2
# Path: /path/to/image.jpg
# Caption: Ваша подпись
```

### 3. Видео кружок (circle)
```python
python test_send_message.py
# Type: 3
# Path: /path/to/video.mp4
```

## 🔐 Безопасность

- ✅ Сессии хранятся локально в `sessions/`
- ✅ API credentials в `.env` (не коммитятся в git)
- ✅ Можно использовать 2FA
- ⚠️ Не делитесь файлами `.session` - это полный доступ к аккаунту!

## 🚦 Следующие шаги

1. ✅ **Базовая отправка работает** → Тестируйте разные типы сообщений
2. ✅ **Добавили в систему** → Используйте через API
3. ✅ **Настройте прокси** → Для масштабирования
4. ✅ **Подключите Google Sheets** → Для production

## 💡 Советы

- Начните с 1-2 тестовых сообщений
- Проверьте лимиты Telegram (~30-40 сообщений/день для новых аккаунтов)
- Для массовой рассылки добавьте задержки (30-60 сек между сообщениями)
- Используйте прокси при >50 сообщений/день

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в терминале
2. Убедитесь что API credentials правильные
3. Проверьте интернет соединение
4. Пересоздайте сессию если устарела

---

**Готово!** Теперь вы можете отправлять реальные сообщения через Telegram! 🚀
