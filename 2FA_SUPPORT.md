# 2FA (Two-Factor Authentication) Support

## Обзор

Система теперь поддерживает аккаунты с включенной двухфакторной аутентификацией (2FA).

**Default password**: `7007` (если пароль не указан явно)

---

## Где используется 2FA

### 1. add_account_from_session_data.py (CLI)

При добавлении аккаунта через AUTH KEY, если сессия не авторизована, система спросит:

```
Try 2FA sign-in? (y/N):
```

Если вы выберете `y`, система запросит пароль 2FA:

```
Enter your 2FA password (default: 7007):
```

**Варианты**:
- Нажмите Enter → использует default пароль `7007`
- Введите свой пароль → использует ваш пароль

### 2. Web Interface (будущая поддержка)

В веб-интерфейсе планируется добавить всплывающее окно для ввода 2FA пароля.

---

## Как это работает

### Сценарий 1: AUTH KEY валиден, 2FA не требуется ✅

```
🔌 Testing connection...
✅ CONNECTION SUCCESSFUL!
```

Система автоматически подключается без запроса пароля.

### Сценарий 2: AUTH KEY валиден, но требуется 2FA 🔐

```
❌ Session is not authorized!
   The AUTH KEY may be invalid or expired

   If this account has 2FA enabled, you can try manual sign-in:
   (Leave blank to skip)

Try 2FA sign-in? (y/N): y
Enter your 2FA password (default: 7007): [Enter или введите пароль]
✅ Successfully signed in with 2FA password!
```

### Сценарий 3: Неправильный пароль 2FA ❌

```
Try 2FA sign-in? (y/N): y
Enter your 2FA password (default: 7007): wrong_password
❌ 2FA sign-in failed: invalid password
```

Session файл удаляется, нужно начать заново с правильным паролем.

---

## Default Password: 7007

**Почему 7007?**
- Указано пользователем как стандартный пароль для аккаунтов
- Упрощает добавление множества аккаунтов с одинаковым паролем
- Можно переопределить, введя другой пароль вручную

**Безопасность**:
- ⚠️ Пароль хранится только локально во время ввода
- ✅ НЕ сохраняется в базу данных
- ✅ НЕ отправляется никуда кроме Telegram API
- ✅ Используется только для аутентификации через Telethon

---

## Использование

### Добавление аккаунта с 2FA через CLI

```bash
cd /path/to/telespam
python3 add_account_from_session_data.py
```

**Шаги**:
1. Введите phone number
2. Введите AUTH KEY (hex)
3. Введите DC ID
4. Введите USER ID (optional)
5. Система создаст session и проверит подключение
6. Если требуется 2FA, выберите `y` и введите пароль (или Enter для `7007`)

### Пример с default паролем

```
Try 2FA sign-in? (y/N): y
Enter your 2FA password (default: 7007): [просто нажмите Enter]
✅ Successfully signed in with 2FA password!
```

### Пример с custom паролем

```
Try 2FA sign-in? (y/N): y
Enter your 2FA password (default: 7007): my_secure_password_123
✅ Successfully signed in with 2FA password!
```

---

## Ошибки и решения

### Error: SessionPasswordNeededError

**Причина**: Аккаунт имеет включенную 2FA, но пароль не введен

**Решение**:
```
Try 2FA sign-in? (y/N): y
Enter your 2FA password (default: 7007): [ваш пароль или Enter]
```

### Error: Invalid password

**Причина**: Неправильный пароль 2FA

**Решение**:
1. Проверьте пароль в Telegram: Settings → Privacy and Security → Two-Step Verification
2. Удалите session file
3. Запустите скрипт заново с правильным паролем

### Error: Session still not authorized after 2FA attempt

**Причина**: AUTH KEY неверный ИЛИ аккаунт забанен/деактивирован

**Решение**:
1. Проверьте правильность AUTH KEY
2. Проверьте, что аккаунт активен в Telegram
3. Попробуйте получить новый AUTH KEY

---

## Web Interface (TODO)

### Планируется добавить:

1. **Popup modal для 2FA**:
   - Появляется автоматически при ошибке `SessionPasswordNeededError`
   - Поле для ввода пароля
   - Default значение `7007` (можно переопределить)
   - Кнопка "Submit" и "Cancel"

2. **В форме добавления аккаунта**:
   - Checkbox "This account has 2FA enabled"
   - Поле "2FA Password" (показывается если checkbox включен)
   - Default value `7007`

### Пример UI

```html
<!-- Modal for 2FA -->
<div id="2fa-modal" class="modal">
  <div class="modal-content">
    <h3>🔐 2FA Password Required</h3>
    <p>This account has Two-Factor Authentication enabled.</p>
    <input type="password" id="2fa-password" placeholder="Enter 2FA password" value="7007">
    <button onclick="submit2FA()">Submit</button>
    <button onclick="cancel2FA()">Cancel</button>
  </div>
</div>
```

---

## API Endpoints (для будущей реализации)

### POST /api/accounts/verify-2fa

**Request**:
```json
{
  "phone": "+79991234567",
  "password": "7007",
  "session_data": {...}
}
```

**Response** (Success):
```json
{
  "success": true,
  "message": "Account authorized with 2FA"
}
```

**Response** (Error):
```json
{
  "success": false,
  "error": "Invalid 2FA password"
}
```

---

## Безопасность

### ✅ Что безопасно:

1. **Пароль не сохраняется**:
   - Используется только для аутентификации
   - НЕ хранится в базе данных
   - НЕ логируется

2. **Encryption**:
   - Telethon использует MTProto для шифрования
   - Пароль передается зашифрованным

3. **Local only**:
   - Пароль вводится локально
   - НЕ передается через веб-интерфейс (пока нет поддержки)

### ⚠️ Рекомендации:

1. **Используйте сильные пароли**:
   - Не используйте `7007` для важных аккаунтов
   - Используйте уникальные пароли для каждого аккаунта

2. **Не делитесь паролями**:
   - Пароль 2FA = полный доступ к аккаунту
   - Храните пароли в password manager

3. **Регулярно меняйте пароли**:
   - Особенно если подозреваете компрометацию

---

## Совместимость

### Поддерживаемые методы добавления аккаунтов:

| Метод | 2FA Support | Status |
|-------|-------------|---------|
| add_account_from_session_data.py (CLI) | ✅ Да | Реализовано |
| add_account_manual.py (CLI) | ✅ Да | Было ранее |
| add_account_cli.py (CLI) | ✅ Да | Было ранее |
| Web Interface | ❌ Пока нет | TODO |

---

## Примеры использования

### Bulk добавление аккаунтов с одинаковым паролем

```bash
#!/bin/bash

# accounts.txt содержит:
# PHONE,AUTH_KEY,DC_ID
# +79991234567,a1b2c3...,2
# +79991234568,d4e5f6...,2

while IFS=',' read -r phone authkey dcid; do
    echo "Adding account: $phone"
    
    # Автоматический ввод с default паролем 7007
    expect << EOF
spawn python3 add_account_from_session_data.py
expect "Phone"
send "$phone\r"
expect "AUTH KEY"
send "$authkey\r"
expect "DC ID"
send "$dcid\r"
expect "USER ID"
send "\r"
expect "Try 2FA sign-in?"
send "y\r"
expect "Enter your 2FA password"
send "\r"
expect eof
EOF

done < accounts.txt
```

---

## Changelog

### v1.1.0 - 2025-12-07

- ✅ Добавлена поддержка 2FA в `add_account_from_session_data.py`
- ✅ Default password: `7007`
- ✅ Fallback при неудачной авторизации
- ✅ Улучшенные сообщения об ошибках
- ⏳ TODO: Web interface support

---

## FAQ

### Q: Почему default пароль `7007`?
**A**: Указано пользователем как стандартный пароль для bulk операций с аккаунтами.

### Q: Могу ли я использовать другой пароль?
**A**: Да! Просто введите свой пароль вместо нажатия Enter.

### Q: Безопасно ли хранить пароль в скрипте?
**A**: ❌ НЕТ! Пароль вводится интерактивно и не хранится нигде.

### Q: Что если я забыл пароль 2FA?
**A**: Восстановите через Telegram: Settings → Privacy and Security → Two-Step Verification → "Forgot Password?"

### Q: Можно ли отключить 2FA?
**A**: Да, в Telegram: Settings → Privacy and Security → Two-Step Verification → Turn Passcode Off

### Q: Поддерживается ли 2FA hint?
**A**: Пока нет, но можно добавить в будущих версиях.

---

**Готово!** ✅ Теперь система поддерживает аккаунты с 2FA.
