# 🧪 Тестирование CSV/XLS импорта

## ✅ Статус: Endpoint работает!

Функция импорта CSV/XLS работает корректно. Тесты показывают:
- ✅ pandas установлен (v2.1.3)
- ✅ openpyxl установлен (v3.1.2)
- ✅ xlrd установлен (v2.0.1)
- ✅ Flask установлен (v3.0.0)
- ✅ Endpoint /users/import-csv работает
- ✅ Импорт CSV файлов успешен

## 🚀 Запуск для тестирования

### Вариант 1: Development сервер (рекомендуется для тестов)

```bash
# Перейти в директорию проекта
cd /home/user/telespam

# Запустить dev сервер
python3 run_dev_server.py
```

Сервер запустится на http://127.0.0.1:5000

### Вариант 2: Production сервер (на сервере)

```bash
# На production сервере:
sudo systemctl restart telespam-web
sudo systemctl status telespam-web

# Смотреть логи:
sudo journalctl -u telespam-web -f
```

## 📝 Тестирование импорта

### Шаг 1: Запустить сервер

```bash
python3 run_dev_server.py
```

### Шаг 2: Открыть браузер

Перейти на http://127.0.0.1:5000

### Шаг 3: Зарегистрироваться / Войти

1. Регистрация: http://127.0.0.1:5000/register
2. Логин: http://127.0.0.1:5000/login

### Шаг 4: Создать кампанию

1. Перейти в "Create New Campaign"
2. Нажать кнопку "Import CSV/XLS"
3. Выбрать тестовый файл: `test_users.csv`
4. Наблюдать процесс импорта

### Шаг 5: Проверить логи в терминале

В терминале где запущен сервер вы увидите:

```
============================================================
CSV IMPORT REQUEST RECEIVED
============================================================
Files in request: ['file']
File received: test_users.csv
✓ Pandas imported successfully (v2.1.3)
✓ CSV read with UTF-8 encoding
DataFrame shape: (3, 4)
Columns: ['username', 'user_id', 'phone', 'priority']
Processing 3 rows...
✓ Import complete: 3 users imported, 0 skipped
============================================================
```

## 🧪 Автоматические тесты

### Тест 1: Проверка зависимостей

```bash
python3 test_csv_import.py
```

Вывод:
```
✓ pandas: 2.1.3
✓ openpyxl: 3.1.2
✓ xlrd: 2.0.1
✓ Successfully read CSV file
✓ CSV import works correctly!
```

### Тест 2: Проверка endpoint

```bash
python3 test_import_endpoint.py
```

Вывод:
```
✓ Correctly requires authentication
✓ CSV IMPORT WORKS!
Response: Success=True, Count=3
```

## 📄 Формат CSV файла

### Пример `test_users.csv`:

```csv
username,user_id,phone,priority
testuser1,123456789,,1
testuser2,987654321,,2
,555444333,+1234567890,1
```

### Поддерживаемые колонки:

| Колонка | Тип | Обязательна | Описание |
|---------|-----|-------------|----------|
| username | Текст | Нет* | Telegram username (без @) |
| user_id | Число | Нет* | Telegram user ID |
| phone | Текст | Нет* | Номер телефона |
| priority | Число | Нет | Приоритет (по умолчанию 1) |

\* Хотя бы одно из полей должно быть заполнено

## 🔍 Отладка

### Проблема: "Ничего не происходит"

**Причины:**
1. ❌ Сервер не запущен
2. ❌ Не залогинены в системе
3. ❌ Обращаетесь не к тому URL

**Решение:**
1. Убедитесь что сервер запущен (`python3 run_dev_server.py`)
2. Откройте консоль браузера (F12 → Console)
3. Проверьте вкладку Network (F12 → Network)
4. Убедитесь что залогинены в системе

### Проблема: Ошибка в консоли браузера

**Откройте консоль браузера (F12) и проверьте:**

1. **Console tab** - ошибки JavaScript
2. **Network tab** - статус запроса к /users/import-csv
   - Статус 200 = OK
   - Статус 302 = Не залогинены
   - Статус 500 = Ошибка сервера

### Проблема: "Required libraries not installed"

**Решение:**

```bash
pip install pandas==2.1.3 openpyxl==3.1.2 xlrd==2.0.1
```

## 📊 Поддерживаемые форматы

| Формат | Расширение | Библиотека | Статус |
|--------|-----------|------------|--------|
| CSV | .csv | pandas | ✅ Работает |
| Excel (новый) | .xlsx | openpyxl | ✅ Работает |
| Excel (старый) | .xls | xlrd | ✅ Работает |

## ✅ Чеклист перед обращением в поддержку

- [ ] Сервер запущен (`python3 run_dev_server.py`)
- [ ] Зарегистрирован/залогинен пользователь
- [ ] Проверена консоль браузера (F12 → Console)
- [ ] Проверена вкладка Network (F12 → Network)
- [ ] Запущены автоматические тесты
- [ ] Проверены логи сервера в терминале

## 📞 Диагностическая информация

Если проблема не решена, соберите:

1. **Вывод тестов:**
   ```bash
   python3 test_csv_import.py > test_output.txt 2>&1
   python3 test_import_endpoint.py >> test_output.txt 2>&1
   ```

2. **Скриншоты:**
   - Console (F12 → Console)
   - Network tab (F12 → Network → запрос к /users/import-csv)

3. **Логи сервера:**
   - Скопируйте вывод из терминала где запущен `run_dev_server.py`

---

**Дата создания:** 2025-11-30
**Версия:** 1.0
**Статус:** ✅ Все тесты пройдены
