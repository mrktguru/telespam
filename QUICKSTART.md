# Quick Start - Deploy to tgspam.mrktgu.ru

## ✅ Что готово

Все файлы для деплоя созданы и отправлены в репозиторий:

- ✅ `deploy.sh` - автоматический скрипт деплоя
- ✅ `telespam-web.service` - systemd конфигурация
- ✅ `nginx-tgspam.conf` - Nginx конфигурация
- ✅ `DEPLOYMENT.md` - полная документация
- ✅ `WEB_README.md` - обновлена инструкция
- ✅ Gunicorn установлен (путь: `/usr/local/bin/gunicorn`)

## 🚀 Запуск (3 команды)

Подключитесь к серверу и выполните:

```bash
# 1. Обновить код
cd /root/telespam/telespam
git pull origin claude/implement-system-from-md-01EpcBwxCBNrC3QRHmtTsEns

# 2. Запустить автоматический деплой
sudo bash deploy.sh

# 3. Установить SSL сертификат
sudo certbot --nginx -d tgspam.mrktgu.ru
```

Готово! Откройте https://tgspam.mrktgu.ru

## 📋 Что делает deploy.sh

1. Проверяет/устанавливает gunicorn
2. Устанавливает Flask 3.0.0
3. Копирует systemd service в `/etc/systemd/system/`
4. Копирует Nginx конфиг в `/etc/nginx/sites-available/`
5. Активирует конфиг в sites-enabled
6. Запускает все службы
7. Показывает статус

## 🔍 Проверка

После деплоя:

```bash
# Проверить статус службы
sudo systemctl status telespam-web

# Смотреть логи
sudo journalctl -u telespam-web -f

# Проверить Nginx
sudo nginx -t
```

Откройте браузер: **https://tgspam.mrktgu.ru**

Должна открыться страница регистрации!

## 🛠️ Если что-то не работает

См. подробную документацию: `DEPLOYMENT.md`

Основные команды отладки:

```bash
# Логи приложения
sudo journalctl -u telespam-web -n 50

# Логи Nginx
sudo tail -f /var/log/nginx/error.log

# Перезапустить службу
sudo systemctl restart telespam-web

# Проверить порт 5000
sudo netstat -tulpn | grep 5000
```

## 🎯 Первый вход

1. Откройте https://tgspam.mrktgu.ru
2. Нажмите "Register"
3. Введите email и пароль
4. Войдите в систему
5. Начните создавать кампании!

**Важно:** Аккаунты добавляются через CLI:
```bash
cd /root/telespam/telespam
python cli_menu.py
# Выберите опцию 1, 1a или 1b
```

Они автоматически появятся в веб-интерфейсе!

## 📞 Поддержка

Проблемы? Проверьте:
- DNS: `nslookup tgspam.mrktgu.ru`
- Firewall: порты 80 и 443 открыты
- Логи: `sudo journalctl -u telespam-web -f`

Детали в `DEPLOYMENT.md`.
