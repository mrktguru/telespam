# Telegram API Configuration
# Get these from https://my.telegram.org
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890

# n8n Webhook URLs
# Configure these in your n8n instance
N8N_WEBHOOK_INCOMING=https://your-n8n.example.com/webhook/incoming
N8N_WEBHOOK_STATUS=https://your-n8n.example.com/webhook/status
N8N_WEBHOOK_ACCOUNT=https://your-n8n.example.com/webhook/account-processed

# Google Sheets Configuration
# Your Google Sheets ID (from the URL)
GOOGLE_SHEETS_ID=1abc...xyz
# Path to Google service account credentials JSON file
GOOGLE_CREDENTIALS_FILE=/app/credentials.json

# Account Limits
MAX_DAILY_ACTIVE=7
MAX_DAILY_WARMING=3
COOLDOWN_HOURS=24

# Proxy Configuration (defaults)
# Set to true to enable proxy globally
PROXY_ENABLED=false
DEFAULT_PROXY_TYPE=socks5
DEFAULT_PROXY_HOST=
DEFAULT_PROXY_PORT=
DEFAULT_PROXY_USER=
DEFAULT_PROXY_PASS=

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Directory Paths (optional, defaults will be used if not set)
# INCOMING_DIR=/app/incoming
# UPLOADS_DIR=/app/uploads
# SESSIONS_DIR=/app/sessions
# TEMP_DIR=/app/temp
# MEDIA_DIR=/app/media
# BACKUPS_DIR=/app/backups
