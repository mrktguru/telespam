# 🔄 Исправление проблемы с кэшем браузера

## ❌ Ошибка

```
Uncaught ReferenceError: handleCampaignCSVUpload is not defined
```

## ✅ Причина

Браузер загрузил **старую версию страницы из кэша**. В старой версии функция называлась `handleCampaignCSVUpload`, а в новой - `handleCSVUpload`.

## 🛠️ Решение

### Вариант 1: Жесткая перезагрузка (рекомендуется)

**Windows/Linux:**
- `Ctrl + Shift + R`
- или `Ctrl + F5`

**Mac:**
- `Cmd + Shift + R`
- или `Shift + Reload button`

### Вариант 2: Очистка кэша

#### Chrome/Edge:
1. `F12` (открыть DevTools)
2. Правый клик на кнопку обновления
3. Выбрать **"Empty Cache and Hard Reload"** (Очистить кэш и жестко перезагрузить)

#### Firefox:
1. `Ctrl + Shift + Delete`
2. Выбрать "Кэшированные изображения и файлы"
3. Нажать "Очистить сейчас"

#### Safari:
1. `Cmd + Option + E` (очистить кэш)
2. Затем `Cmd + R` (обновить)

### Вариант 3: Режим инкогнито

1. Откройте новое окно инкогнито:
   - Chrome: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`
2. Перейдите на страницу создания кампании

### Вариант 4: Добавить версию в URL

Добавьте `?v=2` к URL:
```
http://your-server/campaigns/new?v=2
```

## ✅ Проверка

После очистки кэша:

1. Откройте `F12` (DevTools)
2. Перейдите на вкладку **Console**
3. Попробуйте загрузить CSV файл
4. Ошибка должна исчезнуть

Если видите в консоли:
```
Processing test_users.csv...
```
Значит кэш очищен и все работает!

## 🔍 Как проверить версию JavaScript

В консоли браузера (F12 → Console) выполните:

```javascript
console.log(typeof handleCSVUpload);
```

**Должно быть:** `function`
**Если:** `undefined` - кэш не очищен

## 🚨 Если не помогло

1. **Проверьте, что сервер обновлен:**
   ```bash
   git log -1 --oneline
   # Должно быть: 64f3d25 или новее
   ```

2. **Перезапустите сервер:**
   ```bash
   sudo systemctl restart telespam-web
   ```

3. **Проверьте файл шаблона:**
   ```bash
   grep "handleCSVUpload" /path/to/telespam/templates/new_campaign.html
   # Должно найти функцию handleCSVUpload, НЕ handleCampaignCSVUpload
   ```

## 📝 Для разработчиков

Чтобы избежать проблем с кэшем в будущем, можно добавить версионирование к статическим файлам:

```html
<script src="/static/app.js?v={{ version }}"></script>
```

Или добавить заголовки кэширования в nginx:

```nginx
location ~* \.(js|css)$ {
    expires 1h;
    add_header Cache-Control "public, must-revalidate";
}
```
